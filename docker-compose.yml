services:
  wendy:
    build: .
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
